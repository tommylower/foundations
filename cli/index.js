#!/usr/bin/env node

/**
 * wip-create-designbase
 *
 * Scaffolds a design-first codebase with:
 * - Next.js (app router) + Bun
 * - Tailwind CSS + shadcn/ui
 * - Framer Motion
 * - Supabase (auth, db, edge functions)
 * - TypeScript (strict)
 * - ESLint + Prettier
 * - .agents/ context system (project, architecture, design, tasks, tools)
 * - .claude/ + .cursor/ config pointing to skills library
 * - Skills library linked from tommylower/skills
 * - /rams design review command
 *
 * Usage:
 *   npx wip-create-designbase my-project
 *   npx wip-create-designbase my-project --skip-supabase
 */

console.log("\n  wip-create-designbase is not ready yet.\n");
console.log("  This CLI will scaffold a complete design-first codebase.");
console.log("  Follow progress at: https://github.com/tommylower/foundations\n");

process.exit(0);
